import 'package:flutter/material.dart';

class App extends StatelessWidget {
  Widget build(context) => MaterialApp(
        title: 'Log Me In!',
        home: Scaffold(
          body: Text('Show a for here!'),
        ),
      );
}
